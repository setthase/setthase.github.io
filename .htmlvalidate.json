{
  "$schema": "https://html-validate.org/schemas/config.json",
  "extends": ["html-validate:recommended", "html-validate:prettier"],
  "rules": {
    "no-inline-style": ["error", { "allowedProperties": ["--level"] }]
  }
}
